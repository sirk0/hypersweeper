node_modules/
dist/
dev-dist/
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
# ...and where ci.yml's merge job downloads every shard's blob-report to.
/all-blob-reports/
/.playwright/
/.vite/

# wrangler (the Pages Function's local state and secrets)
/.wrangler/
/.dev.vars
